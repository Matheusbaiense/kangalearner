{
  "ignoreCommand": "git rev-parse HEAD~1 >/dev/null 2>&1 || exit 1; git diff HEAD~1 --name-only | grep -qE '^apps/web|^packages/core' && exit 1 || exit 0",
  "crons": [
    {
      "path": "/api/ping",
      "schedule": "0 6 */3 * *"
    }
  ]
}
