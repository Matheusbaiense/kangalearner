import type { ReactNode } from "react";

export const metadata = {
  title: "WA Learner Journey Resources",
  description:
    "Theory test, driving hours, HPT, PDA and official WA licensing links — the full learner journey in one place."
};

export default function ResourcesLayout({ children }: { children: ReactNode }) {
  return children;
}
