import type { ReactNode } from "react";

export const metadata = {
  title: "Learn WA Road Rules",
  description:
    "Study all 19 WA road rule topics — signs, speed limits, give way, alcohol laws and more. In English, Portuguese or Spanish."
};

export default function LearnLayout({ children }: { children: ReactNode }) {
  return children;
}
