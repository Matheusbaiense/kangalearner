import type { ReactNode } from "react";

export const metadata = {
  title: "WA Mock Test — 30 Questions",
  description:
    "Simulate the official WA learner test: 30 questions, pass mark 24/30. Practice or exam mode, free."
};

export default function MockTestLayout({ children }: { children: ReactNode }) {
  return children;
}
