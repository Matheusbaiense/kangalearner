# Mobile App Status

**Status: Scaffold — Not in active development**

This directory is a placeholder for a future React Native / Expo app.

Current state: empty scaffold generated by `create-expo-app`.
No components, no navigation, no API integration.

## Timeline

Mobile development is planned for after the web app reaches feature parity
across all Australian states (NSW, VIC, QLD, SA, TAS, ACT, NT).

## Notes for Turbo

If you want to exclude this from turbo pipelines until active development starts,
add `apps/mobile` to `.turboignore` in the repo root.
