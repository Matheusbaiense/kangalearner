name: Backup

on:
  workflow_dispatch:
  schedule:
    - cron: "17 18 * * *"

permissions:
  contents: read

concurrency:
  group: supabase-backup
  cancel-in-progress: false

jobs:
  supabase-postgres:
    name: Supabase Postgres backup
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
      BACKUP_ENCRYPTION_KEY: ${{ secrets.BACKUP_ENCRYPTION_KEY }}
      BACKUP_BUCKET: ${{ secrets.BACKUP_BUCKET }}
      BACKUP_S3_ENDPOINT: ${{ secrets.BACKUP_S3_ENDPOINT }}
      AWS_ACCESS_KEY_ID: ${{ secrets.BACKUP_AWS_KEY }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.BACKUP_AWS_SECRET }}
      AWS_DEFAULT_REGION: auto

    steps:
      - name: Validate backup configuration
        run: |
          missing=0
          for name in SUPABASE_DB_URL BACKUP_ENCRYPTION_KEY BACKUP_BUCKET BACKUP_S3_ENDPOINT AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY; do
            if [ -z "${!name}" ]; then
              echo "::error::$name is not configured"
              missing=1
            fi
          done
          if [ "$missing" -ne 0 ]; then
            exit 1
          fi

      - name: Install backup tools
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends gnupg unzip lsb-release wget ca-certificates
          # Ubuntu's default apt repos only carry postgresql-client 16; Supabase runs
          # Postgres 17, and pg_dump refuses to dump a server newer than itself. Add
          # PGDG (the upstream Postgres apt repo) to get a matching client version.
          sudo install -d /usr/share/postgresql-common/pgdg
          sudo wget -qO /usr/share/postgresql-common/pgdg/apt.postgresql.org.asc \
            https://www.postgresql.org/media/keys/ACCC4CF8.asc
          echo "deb [signed-by=/usr/share/postgresql-common/pgdg/apt.postgresql.org.asc] https://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" \
            | sudo tee /etc/apt/sources.list.d/pgdg.list
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends postgresql-client-17
          curl -sS "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o /tmp/awscliv2.zip
          unzip -q /tmp/awscliv2.zip -d /tmp
          sudo /tmp/aws/install --update

      - name: Dump database
        run: |
          mkdir -p backups
          ts="$(date -u +'%Y%m%dT%H%M%SZ')"
          dump="backups/kangalearner-supabase-${ts}.dump"
          /usr/lib/postgresql/17/bin/pg_dump \
            --dbname "$SUPABASE_DB_URL" \
            --format=custom \
            --no-owner \
            --no-acl \
            --file "$dump"
          echo "DUMP_FILE=$dump" >> "$GITHUB_ENV"
          echo "ENCRYPTED_FILE=${dump}.gpg" >> "$GITHUB_ENV"

      - name: Encrypt dump
        run: |
          gpg \
            --batch \
            --yes \
            --pinentry-mode loopback \
            --passphrase "$BACKUP_ENCRYPTION_KEY" \
            --symmetric \
            --cipher-algo AES256 \
            --output "$ENCRYPTED_FILE" \
            "$DUMP_FILE"
          rm -f "$DUMP_FILE"

      - name: Upload encrypted backup
        run: |
          ls -la "$ENCRYPTED_FILE"
          aws s3 cp \
            "$ENCRYPTED_FILE" \
            "s3://${BACKUP_BUCKET}/supabase/$(basename "$ENCRYPTED_FILE")" \
            --endpoint-url "$BACKUP_S3_ENDPOINT" \
            --debug 2>&1 | tail -100

      - name: Verify uploaded backup is listed
        run: |
          aws s3 ls \
            "s3://${BACKUP_BUCKET}/supabase/$(basename "$ENCRYPTED_FILE")" \
            --endpoint-url "$BACKUP_S3_ENDPOINT"
