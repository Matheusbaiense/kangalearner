# Backlog — KangaLearner

> **O backlog canônico vive em [`docs/BACKLOG.md`](docs/BACKLOG.md).**
>
> Este arquivo era a versão original e acumulou seções desatualizadas — em
> particular, a antiga seção "🔴 CRÍTICO — Drift de schema (prod ≠ migrations)"
> descrevia um problema **já resolvido** pela migração
> `supabase/migrations/025_reconcile_prod_attempts_category_stats.sql`
> (verificada em produção em 2026-06-01: `question_attempts.attempt_id` e
> `user_category_stats` existem). Não há drift de schema pendente; não
> "re-corrija" isso.
>
> Para o histórico do site estático pré-Next.js, ver o git history deste
> arquivo (`git log -- BACKLOG.md`).
