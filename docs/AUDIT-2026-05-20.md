# KangaLearner — Auditoria de Engenharia Completa
**Data:** 2026-05-20  
**Fontes:** Claude Code (4 agentes paralelos: API Routes, React Components, Database, Monorepo) + Cursor (5 auditorias: dead code, arquitetura, code review, segurança, code quality)  
**Status:** Itens com ✅ foram corrigidos nesta sessão

---

## Resumo Executivo

| Severidade | Total | Corrigidos nesta sessão | Pendentes |
|-----------|-------|------------------------|-----------|
| 🔴 CRITICAL | 5 | 3 | 2 |
| 🟠 HIGH | 16 | 4 | 12 |
| 🟡 MEDIUM | 17 | 1 | 16 |
| 🔵 LOW | 9 | 2 | 7 |

---

## 🔴 CRITICAL

### C1 — `handleDeleteAccount` faz apenas signOut — não apaga a conta
**Arquivo:** `apps/web/app/account/page.tsx:112`  
**Impacto:** Promessa falsa; possível violação de GDPR/LGPD  
**Fix:** Chamar `supabase.auth.admin.deleteUser(user.id)` via route handler protegido  
**Status:** ❌ Pendente (Cursor)

### C2 — Migration `0001_auth_account_product_schema.sql` cria 13 tabelas conflitantes  
**Arquivo:** `supabase/migrations/0001_auth_account_product_schema.sql`  
**Impacto:** Se `supabase db push` rodar em ordem alfabética, destrói a schema de produção  
**Fix:** Renomear para `DRAFT_0001_auth_account_product_schema.sql`  
**Status:** ❌ Pendente (Cursor)

### C3 — `newsletter_subscribers` não tinha migration — usava `as any`  
**Arquivo:** `apps/web/app/api/newsletter/route.ts:15`  
**Fix:** Migration 013 criou a tabela com RLS; remover `as any` e usar tipos gerados  
**Status:** ✅ Tabela criada via MCP (013_security_hardening_rls_newsletter.sql). Remover `as any` pendente (Cursor)

### C4 — `CRON_SECRET` com guard invertido — `/api/ping` era público sem a variável  
**Arquivo:** `apps/web/app/api/ping/route.ts:7`  
**Fix:** `if (!cronSecret || authHeader !== ...)` + documentado no `.env.example`  
**Status:** ✅ Corrigido

### C5 — Admin dashboard sem verificação de role server-side  
**Arquivo:** `apps/web/app/admin/layout.tsx`  
**Impacto:** Qualquer usuário autenticado via `useEffect` client-side recebia o HTML do painel antes do redirect  
**Fix:** Convertido para Server Component — verifica role antes de renderizar  
**Status:** ✅ Corrigido

---

## 🟠 HIGH

### H1 — Email pessoal hardcoded em migration SQL (git history permanente)
**Arquivo:** `supabase/migrations/011_roles_expansion.sql:18`  
`update profiles set role = 'super_admin' where id = (select id from auth.users where email = 'baiensem@gmail.com' limit 1);`  
**Fix:** Substituir por variável de ambiente ou instrução manual documentada  
**Status:** ❌ Pendente (Cursor)

### H2 — Nenhuma rota pública tem rate limiting
**Arquivos:** `api/newsletter`, `api/attempts`, `api/attempts/bulk`, `api/mock-sessions`  
**Fix:** Implementar `@upstash/ratelimit` ou Vercel Edge Rate Limiting  
**Status:** ❌ Pendente (Cursor)

### H3 — `is_admin()` não incluía `super_admin` — todas as tabelas protegidas por ela bloqueavam super_admins  
**Fix:** Função recriada via migration 013  
**Status:** ✅ Corrigido no Supabase

### H4 — RLS chamava `auth.uid()` por linha ao invés de `(SELECT auth.uid())`  
**Impacto:** Full table scan penalizado — cresce com volume  
**Fix:** Todas as policies recriadas com `(SELECT auth.uid())` na migration 013  
**Status:** ✅ Corrigido no Supabase

### H5 — Nenhum CSP header em `next.config.ts`  
**Fix:** CSP + HSTS adicionados; dev usa CSP permissivo, prod usa CSP restrito  
**Status:** ✅ Corrigido

### H6 — Dashboard faz `SELECT *` em `question_attempts` sem LIMIT
**Arquivo:** `apps/web/app/dashboard/page.tsx`  
**Fix:** Adicionar `.limit(500)` e paginação progressiva  
**Status:** ❌ Pendente (Cursor)

### H7 — Admin stats: 4 queries sem `GROUP BY` — processamento em JS
**Arquivo:** `apps/web/app/api/admin/stats/route.ts`  
**Fix:** Substituir por RPCs PostgreSQL com `GROUP BY`  
**Status:** ❌ Pendente (Cursor)

### H8 — Admin users: N+1 roundtrips para buscar emails (50 chamadas por page load)
**Arquivo:** `apps/web/app/api/admin/users/route.ts:49-63`  
**Fix:** Usar `listUsers()` uma vez ou sincronizar email no `profiles` via trigger  
**Status:** ❌ Pendente (Cursor)

### H9 — `selectedState` hardcoded `"WA"` — seletor de estado no nav é decorativo
**Arquivo:** `apps/web/app/practice/PracticeClient.tsx`, `SiteNav.tsx`  
**Fix:** Persistir estado selecionado em localStorage/contexto, propagar para PracticeClient  
**Status:** ❌ Pendente (Cursor)

### H10 — Dois schemas de localStorage divergentes (static site vs Next.js)
**Arquivos:** `assets/js/storage.js`, `apps/web/src/lib/migrateLocalAttempts.ts`  
**Fix:** Extrair constantes de keys para `@kanga/core`  
**Status:** ❌ Pendente (Cursor)

### H11 — `apps/mobile` é scaffold vazio — implica readiness que não existe
**Arquivo:** `apps/mobile/`  
**Fix:** Adicionar ao `.turboignore` ou criar `apps/mobile/STATUS.md`  
**Status:** ❌ Pendente (Cursor)

### H12 — `database.types.ts` desatualizado (mock_sessions.id como number, 7 colunas faltando)  
**Fix:** Regenerado via Supabase MCP nesta sessão  
**Status:** ✅ Corrigido (PR #50)

### H13 — `as any` em mock-test session/results — bypassa type-safety do banco
**Arquivos:** `apps/web/app/mock-test/`  
**Fix:** Usar tipos corretos após regeneração do `database.types.ts`  
**Status:** ❌ Pendente (Cursor)

### H14 — `assertAdmin` e `assertSuperAdmin` são funções idênticas copiadas em 2 arquivos
**Arquivos:** `api/admin/stats/route.ts`, `api/admin/users/route.ts`  
**Fix:** Extrair para `src/lib/auth/assertAdminRole.ts`  
**Status:** ❌ Pendente (Cursor)

### H15 — Erros Supabase (`error.message`) retornados diretamente ao client
**Arquivos:** `admin/users`, `newsletter`, `ping` (ping corrigido)  
**Fix:** Log interno, resposta genérica ao client  
**Status:** ✅ Parcialmente corrigido em `ping`. Pendente: `admin/users`, `newsletter` (Cursor)

### H16 — `/api/attempts/bulk` aceita arrays sem limite de tamanho
**Arquivo:** `apps/web/app/api/attempts/bulk/route.ts:46`  
**Fix:** `if (attempts.length > 500) return 400`  
**Status:** ❌ Pendente (Cursor)

---

## 🟡 MEDIUM

### M1 — `FlagImg` duplicado em 3 arquivos
`SiteNav.tsx`, `LandingClient.tsx`, `Footer.tsx` → extrair para `src/components/ui/FlagImg.tsx`

### M2 — `tx()` helper duplicado em 5 arquivos
→ extrair para `src/lib/i18n.ts`

### M3 — `MockConfig`/`MockSession` types duplicados em 2 lugares
→ mover para `@kanga/core` ou `src/types/mock.ts`

### M4 — Dashboard e Account sem i18n — hardcoded em inglês
`apps/web/app/dashboard/`, `apps/web/app/account/`

### M5 — `mock-test/page.tsx` usa strings i18n fora do sistema central

### M6 — `DELETE /api/profile/avatar` ignora todos os erros de storage
`apps/web/app/api/profile/avatar/route.ts:96`

### M7 — `POST /api/profile/avatar` ignora erro do DB update
`apps/web/app/api/profile/avatar/route.ts:68`

### M8 — `state` aceita qualquer string — nenhuma validação contra lista de estados AU
`api/attempts/route.ts`, `api/mock-sessions/route.ts`

### M9 — `PATCH /api/admin/users` sem try/catch em `req.json()`
`apps/web/app/api/admin/users/route.ts:72`

### M10 — `supabaseAdmin` Proxy recria o cliente em cada acesso de propriedade
`apps/web/src/lib/supabase/admin.ts:26-32`

### M11 — `mock-sessions` hardcoded `country: "AU"`
`apps/web/app/api/mock-sessions/route.ts:58`

### M12 — `@stripe/stripe-js` declarado mas nunca importado
`apps/web/package.json`

### M13 — `score > total` não validado em `mock-sessions`
`apps/web/app/api/mock-sessions/route.ts`

### M14 — `AuthCard.tsx` nunca referenciado — dead component
`apps/web/src/components/auth/AuthCard.tsx` → deletar

### M15 — `api/attempts/bulk/route.ts` — endpoint sem caller no código
Nenhum arquivo chama `/api/attempts/bulk` → deletar ou documentar  
**Atenção:** `sync-local-progress.ts` deveria usar este endpoint

### M16 — Passmark contraditório: 80% vs 87%
`apps/web/src/lib/learnTopics.ts:37` vs `LandingClient.tsx:107`  
→ verificar DoT WA e centralizar em `@kanga/core`

### M17 — `user_settings` tinha 6 policies duplicadas (2 conjuntos sobrepostos)
✅ Corrigido via migration 013 — consolidado em 3 policies

---

## 🔵 LOW

### L1 — `CRON_SECRET` ausente do `.env.example` ✅ Corrigido
### L2 — `smoke-static-site.puppeteer.mjs` sem `puppeteer` em devDependencies → deletar
### L3 — `designer/` com ZIP binário não está no `.gitignore` → adicionar
### L4 — `src/main.js` + `src/js/config.js` não carregados pelo `index.html` de produção → mover para `scripts/`
### L5 — `assets/js/dev/validate-questions.js` em pasta de produção → mover para `scripts/`
### L6 — `console.error` com `error.details` em 3 route handlers → usar logger estruturado
### L7 — `marketplace_waitlist` tem INSERT aberto para anônimos → adicionar captcha ou limite
### L8 — Middleware `AUTH_ROUTES` inclui paths legados `/login`, `/signup` → confirmar se ainda existem
### L9 — `currentPwd` coletado mas ignorado no form de segurança → implementar `reauthenticate()` ou remover campo

---

## Plano de Ação por Sprint

### Sprint 1 — Segurança Crítica (Esta sessão + Cursor)
- [x] C4: CRON_SECRET guard
- [x] C5: Admin layout Server Component
- [x] H3: is_admin() corrigido
- [x] H4: RLS policies com (SELECT auth.uid())
- [x] H5: CSP + HSTS headers
- [x] H12: database.types.ts regenerado
- [ ] C1: Account deletion real
- [ ] C2: Renomear migration DRAFT_0001
- [ ] C3: Remover `as any` de newsletter route
- [ ] H16: Limite 500 no bulk endpoint
- [ ] H1: Email hardcoded na migration

### Sprint 2 — Performance & Integridade de Dados
- [ ] H6: Dashboard LIMIT em question_attempts
- [ ] H7: Admin stats GROUP BY no SQL
- [ ] H8: Admin users sem N+1
- [ ] H14: Extrair assertAdminRole()
- [ ] M9: try/catch em req.json()
- [ ] M10: Singleton para supabaseAdmin
- [ ] M13: Validar score <= total
- [ ] M8: Validar state contra lista AU

### Sprint 3 — Manutenibilidade & DRY
- [ ] M1: Extrair FlagImg
- [ ] M2: Extrair tx()
- [ ] M3: Tipos MockConfig no @kanga/core
- [ ] H9: Conectar seletor de estado
- [ ] M4: i18n no Dashboard e Account
- [ ] M14: Deletar AuthCard.tsx
- [ ] M16: Unificar passmark

### Sprint 4 — Produto & UX
- [ ] C1: Deleção real de conta (GDPR)
- [ ] M6/M7: Error handling no avatar route
- [ ] H11: Status do mobile app
- [ ] L2-L5: Cleanup de arquivos legados
- [ ] H2: Rate limiting (@upstash/ratelimit)

---

## Estado do Banco de Dados (verificado 2026-05-20)

| Tabela | RLS | Policies OK |
|--------|-----|-------------|
| `profiles` | ✅ | ✅ (migration 013) |
| `mock_sessions` | ✅ | ✅ (migration 013) |
| `question_attempts` | ✅ | ✅ (migration 013) |
| `user_settings` | ✅ | ✅ (migration 013, duplicates removed) |
| `newsletter_subscribers` | ✅ | ✅ (criada na migration 013) |

## Migrations Aplicadas em Produção

| Migration | Status |
|-----------|--------|
| 001–012 | ✅ Aplicadas anteriormente |
| 013_security_hardening_rls_newsletter | ✅ Aplicada 2026-05-20 |

**ATENÇÃO:** `0001_auth_account_product_schema.sql` existe na pasta de migrations mas está marcado "Do NOT apply". Renomear para `DRAFT_0001_*` antes de qualquer `supabase db push`.
